# Docker Infrastructure

Docker Compose files and service Dockerfiles will live here.

Planned files:

- `docker-compose.dev.yml`
- `docker-compose.prod.yml`
- `Dockerfile.api-gateway`
- `Dockerfile.service`
- `Dockerfile.web`
